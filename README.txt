This protocol takes the IP addr and port of the server for the client. It also must have the type of message specified by command line to send to the server. There are three possible message type, "update","request", and "response". Any other message type will be cause the client to stop. The server takes and deciphersthe message and acts accordingly.

...Below not finished

Command line to compile server program:

Command line to run server program (including arguments, if any)

Command line to compile client program

Command line to run client program (including arguments, if any)


